{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo '=== RAILWAY BUILD START ===' && echo '=== SWITCHING TO PRODUCTION DEPS ===' && cp package-railway.json package.json && npm install --only=production && echo '=== BUILD COMPLETE ===' && ls -la dist/ && echo 'Using pre-built frontend from git'",
    "watchPatterns": ["**/*.js", "**/*.ts", "**/*.tsx", "railway-server.js", "package.json", "src/**/*", "dist/**/*"]
  },
  "deploy": {
    "startCommand": "node railway-server.js",
    "restartPolicyType": "always",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  },
  "environments": {
    "production": {
      "variables": {
        "NODE_ENV": "production"
      }
    }
  }
} 