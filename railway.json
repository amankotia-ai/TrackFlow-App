{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo '=== RAILWAY BUILD START ===' && npm install && echo '=== BUILDING FRONTEND ===' && npm run build && echo '=== FRONTEND BUILT ===' && ls -la dist/ && echo '=== COPYING TO ROOT ===' && cp dist/index.html index.html && cp -r dist/assets . && echo '=== ROOT FILES COPIED ===' && ls -la index.html assets/ && echo '=== SWITCHING TO PRODUCTION DEPS ===' && cp package-railway.json package.json && npm install --only=production && echo '=== BUILD COMPLETE - ROOT CHECK ===' && ls -la index.html && echo 'Frontend files ready in root directory'",
    "watchPatterns": ["**/*.js", "**/*.ts", "**/*.tsx", "railway-server.js", "package.json", "src/**/*", "index.html", "assets/**/*"]
  },
  "deploy": {
    "startCommand": "node railway-server.js",
    "restartPolicyType": "always",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  },
  "environments": {
    "production": {
      "variables": {
        "NODE_ENV": "production"
      }
    }
  }
} 