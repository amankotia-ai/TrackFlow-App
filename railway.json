{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "echo '=== RAILWAY BUILD START ===' && npm install && echo '=== BUILDING FRONTEND ===' && npm run build && echo '=== FRONTEND BUILT ===' && ls -la dist/ && echo '=== SWITCHING TO PRODUCTION DEPS ===' && cp package-railway.json package.json && npm install --only=production && echo '=== BUILD COMPLETE - DIST CHECK ===' && ls -la dist/ && echo 'dist/index.html exists:' && ls -la dist/index.html",
    "watchPatterns": ["**/*.js", "**/*.ts", "**/*.tsx", "railway-server.js", "package.json", "src/**/*"]
  },
  "deploy": {
    "startCommand": "node railway-server.js",
    "restartPolicyType": "always",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  },
  "environments": {
    "production": {
      "variables": {
        "NODE_ENV": "production"
      }
    }
  }
} 